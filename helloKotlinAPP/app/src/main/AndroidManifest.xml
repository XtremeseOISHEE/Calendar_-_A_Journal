<manifest package="com.example.hellokotlinapp" xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET"/>
    <!-- Android 13+ notification permission (request at runtime) -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:allowBackup="true"
        android:label="Interactive Journal"
        android:theme="@style/Theme.Material3.DayNight.NoActionBar">

        <!-- Alarm receiver for event reminders -->
        <receiver
            android:name=".notification.AlarmReceiver"
            android:exported="false" />

        <!-- FileProvider for image attachment (journals) -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <activity android:name=".ui.auth.LoginActivity" />
        <activity android:name=".ui.auth.SignupActivity" />
        <activity android:name=".ui.event.CreateEventActivity" />
        <activity android:name=".ui.journal.CreateJournalActivity" />
        <activity android:name=".ui.event.EventListActivity" />
        <activity android:name=".ui.journal.JournalListActivity" />
        <activity android:name=".MainActivity2" />

        <activity android:name=".MainActivity"
                  android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

    </application>
</manifest>
